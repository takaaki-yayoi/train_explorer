# 分身の旅日記 v2 — パーソナライズ(自分の分身)叩き台

> ステータス: **ドラフト / 議論用**。実装着手ではなく「刺さったら作る」ための設計の下書き。
> 推奨案には ✅、決めるべき論点には **[決めどころ]** を付す。v1(公共の便)は現状のまま稼働。

## 0. v1との関係

| | v1 (公共の便) | v2 (自分の分身) |
|---|---|---|
| 誰が旅する | 共有キャラ(クロ/ぽこ…) | **ユーザー自身が作った分身** |
| 便 | 全員が同じ1本/日 | 各ユーザーの分身ごと |
| 認証 | なし | 必要(識別・履歴・課金) |
| 永続 | JSONファイル | DB |
| コスト | 全員で1本を按分 | ユーザー×旅の回数に比例 |

v1は「見知らぬ旅人からの便り」。v2で初めて **「分身」=あなたの化身** が本当の意味を持つ。
→ v2を具体化することが、看板に「分身」を使う妥当性の判断にも直結する。

## 1. コアコンセプト

ユーザーが自分の分身を作る:**名前・絵文字・性格(興味・文体)**。
その分身が日本の実在の路線を旅し、性格を通した一人称の日記を書く。
ユーザーは日記を受け取り、**自分の分身が日本をどこまで旅したか**を個人の全国マップに貯めていく。

v1で作った資産(実線形パイプライン・スポット収集・日記生成・地図ビューア・全国カバレッジマップ・OGP)は**そのまま流用**。変わるのは「誰の分身が / 何本 / どう発火するか」。

## 2. 体験フロー

1. **分身を作る**(サインアップ不要で開始 ✅ 下記6参照)
   - 名前 / 絵文字 / ひとこと性格(例:「廃線と橋梁が好きな無口な猫」)
   - 3〜4個のプリセット(v1のクロ/ぽこ等)から選んで微調整も可
2. **旅に出す**
   - 「旅に出す」ボタン、または自動(ログイン+有料で毎朝)
   - 路線はおまかせ(未踏優先)/ 地域指定 / 路線指定 **[決めどころ:自由度をどこまで]**
3. **日記が届く**(生成 1〜2分。プッシュ通知 or 「できたら教える」)
4. **集める**
   - 分身の旅を個人の全国マップに蓄積(= v1トップの個人版)
   - 履歴・訪問路線・累計km
5. **シェア**(OGPカードは v1 の仕組みを流用、分身名入り)

## 3. 生成モデルと課金 ← 最重要の決めどころ

各旅 = LLM 1〜2コール(=コスト)。v1は1本を全員で按分できたが、v2は**ユーザー×旅の本数**でコストが伸びる。だから「分身管理を有料」。

### 発火モデル(案)
- **A. 自動(毎朝)**: 分身が勝手に1路線/日。旅かえる的な"非同期の贈り物"の魅力が最大。ただしコストが線形に増える。
- **B. 手動**: 「旅に出す」で発火。オンデマンド。無料枠で回数制限しやすい。
- **C. ハイブリッド ✅(推奨)**: 無料はゆっくり自動 or 少量手動、有料で毎朝自動+制御。

### 推奨する無料/有料ライン
- **無料**: 分身1体 / 自動は週1ペース(or 手動 週数回まで)/ 標準モデル。
  → "自分の分身から時々便りが届く"を体験できる最小。
- **有料(月額) [決めどころ:価格]**:
  - 毎日自動で旅
  - 分身を複数体
  - 路線・地域を選べる
  - 履歴・全国マップ無制限
  - (option)より良いモデル/長め日記/通知

「分身**管理**を有料」= 複数体・任意発火・こだわり設定が有料、という線引き ✅

**[決めどころ]** 価格帯(例:¥300〜500/月?)、無料枠の具体値(週1?体1?)、年額割引の有無。

## 4. 認証(段階的に)

摩擦を最小化するため段階導入 ✅:
1. **サインアップ不要で分身作成**(デバイスローカル / 匿名IDに紐づけ)。すぐ体験できる。
2. **保存・複数端末・課金はログイン**(「この分身を残すにはログイン」)。
3. ログイン手段 **[決めどころ]**:マジックリンク(メール)/ Sign in with Apple・Google / パスキー。cozyなトーンならマジックリンク or Apple/Googleが軽い。

## 5. データモデル(v2でDBが要る)

- `users`(id, auth, subscription status, created)
- `personas`(id, user_id, name, emoji, traits, model?, created)
- `trips`(id, persona_id, date, line_cd, trip_json_ref, created)
- `visited_lines`(user_id/persona_id, line_cd, first_visited) ← 個人マップ集計用
- `line_track_cache`(line_cd → 実線形)← v1のキャッシュを共有(全ユーザー共通、610路線のみ)

線形キャッシュは全ユーザーで共有できる(実路線は約610本)ので、コストが増えるのは**日記生成(LLM)だけ**。ここは重要な救い。

## 6. 技術構成(Cloudflareネイティブで一貫)

v1は静的+夜間Actionだったが、v2は動的バックエンドが要る。既にCloudflareなので:

- **Cloudflare Workers**: アプリ/API
- **D1 (SQLite)**: users / personas / trips / visited_lines
- **KV or R2**: trip JSON・OGP画像の保管/配信
- **Queues + Cron**: 旅生成を非同期ジョブに(発火→キュー→生成→保存→通知)
- **Stripe**: 課金
- **認証**: マジックリンク or OAuth(軽量ライブラリ / Cloudflare Access)

生成ワーカーは v1 の `lib/`(stations/track/spots/diary)をそのまま呼べる設計。ビューアも v1 をほぼ流用(データ取得先が静的JSON→APIに変わるだけ)。

## 7. 安全性(ユーザーの性格プロンプトを扱う)

ユーザーが自由文で性格を書く → LLMに渡る。リスクと対策:
- **プロンプトインジェクション**: ユーザー文は「データ」として囲い、システムプロンプトを上位に固定。ユーザー文が生成ルール(捏造禁止・リンク規則等)を上書きできないようにする。
- **不適切ペルソナ/出力**: 入力・出力の両方でモデレーション。禁止語・攻撃的設定を弾く。
- **入力の構造化**: 完全自由文でなく「興味の対象 / 文体」等の枠+短い自由文に制限すると安全と品質が両立。
- **公開範囲 [決めどころ]**: 他人の分身の旅は見えるのか(公開ギャラリー)/ 自分だけか。公開するならモデレーション必須。

## 8. v2 の MVP スコープ(最小で出す範囲)

一気に全部作らず、"刺さるか"を測る最小:
1. 分身1体を作る(名前・絵文字・性格、サインアップ不要)
2. 「旅に出す」で1本生成 → 表示(v1ビューア流用)
3. 個人の履歴+全国マップ(訪問路線が貯まる)
4. シェア(OGP流用)
5. ログイン(残す/複数端末)
6. **課金は次段**(まず無料で反応を見る、あるいは最初からStripeで1プランだけ)**[決めどころ]**

自動毎朝・複数体・地域指定・通知は "刺さった後" の拡張として温存。

## 9. 決めどころ一覧(ここが埋まれば仕様が閉じる)

- [ ] 発火モデルと無料/有料ライン(3章):週1自動?手動枠?価格?
- [ ] 認証手段(4章):マジックリンク / Apple・Google / パスキー
- [ ] 路線選択の自由度(2章):おまかせのみ / 地域 / 路線指定
- [ ] 公開範囲(7章):自分だけ / 公開ギャラリーあり
- [ ] 課金をMVPに入れるか、次段か(8章)
- [ ] 分身の上限体数、旅の頻度上限

## 10. 「分身」呼称について

この仕様(**自分でキャラを作って旅させる**)なら「分身」は妥当。むしろ核。
逆に v2 に進まず v1(見知らぬ旅人の便り)を主軸に続けるなら、「分身」は看板から引いた方が素直。
→ **v2をやるかどうかが、名前を「分身」で固定するかの判断そのもの。**

---

### 次アクション(このドラフトに対して)
上の「決めどころ」を埋めていけば、v2の姿・名前・課金・技術がまとまって決まる。
まずは 3章(発火モデル/課金)と 4章(認証)から潰すのがおすすめ。
