# プロダクト概要

「分身の旅日記」— 自分の分身 (キャラクター) が日本の実在の鉄道路線を旅し、途中下車した駅や車窓の風景に感想を残す。ユーザーはその旅日記を地図と一緒に眺める。

体験の核は「操作する旅」ではなく「分身が勝手にした旅の報告を受け取る」非同期体験 (参考: 旅かえるのヒット)。差別化要素は2つ:

1. 実在の路線網を実データ (駅座標・実線形) で旅する具体性
2. LLMがキャラクターの性格を通して、沿線の実在スポットを織り込んだ一人称の日記を生成する

プロトタイプ3本 (reference/ 参照) で以下を検証済み:

- 地図上の移動に日記が付くと「移動」が「旅」になり、体験が成立する
- キャラクター (ペルソナ) を変えると文体だけでなく「何に目を留めるか」が変わり、同じ路線でも別の旅になる (第2便: 食いしん坊ののんびり屋 / 第3便: 廃線と橋梁好きの無口な猫)
- 固有名詞リンクにより「読む→気になる→調べる」の導線が機能する

# v1スコープ: 公共の便

アカウントなしで始める。毎朝1本、全ユーザーが同じ旅日記を読む「新聞連載」形式。

- 夜間バッチが翌朝の便を生成 (路線ランダム選定 + キャラローテーションまたは固定)
- 日記ページは静的URL (例: /trips/2026-07-13) で共有可能。SNS共有が拡散の仕組み
- 認証・DB上のユーザー状態・ユーザー別コストがすべて不要
- PWA対応 (ホーム画面追加、後日Webプッシュ)

v2以降 (刺さった場合): ユーザーごとの分身作成 (名前・絵文字・性格プロンプト)、旅の履歴、訪問路線の記録。分身管理を有料機能とする。

# データモデル

プロトタイプで確定したJSON構造。1つの旅 (trip) は以下の3点セット:

```json
{
  "trip": {
    "date": "2026-07-12",
    "line": {"line_cd": 32005, "name": "南海加太線", "company": "南海電鉄", "km": 12.0},
    "persona": {"name": "クロ", "emoji": "🐈‍⬛", "traits": "廃線と橋梁に目がない無口な猫"},
    "stations": [{"name": "和歌山市", "lat": 34.236925, "lon": 135.166538}],
    "track": [[34.236731, 135.166576], [34.237413, 135.167586]],
    "diary": [
      {"t": "10:02", "st": 0, "type": "stop", "text": "和歌山市駅。今日の電車は<a href=...>めでたいでんしゃ</a>..."}
    ]
  }
}
```

- stations: 駅データ.jpのCSVからline_cdで抽出、e_sort順 (= 路線の並び順)
- track: 実線形の座標列 [lat, lon]。Douglas-Peuckerで200点前後に間引き済み
- diary: type は "stop" (途中下車、長文) / "pass" (車窓から、短文)。st は stations のインデックス。text はHTML (固有名詞リンク入り)

# データソース

data/ に同梱の駅データ.jp無料データ (2026-06-18版ほか):

- station20260618free.csv: 全国約1万駅。列: station_cd, station_g_cd, station_name, line_cd, pref_cd, address, lon, lat, open_ymd, close_ymd, e_status, e_sort
- line20260618free.csv: 610路線。列: line_cd, company_cd, line_name ほか
- company20260409.csv: 事業者。company_cd → company_name
- join20260618.csv: 隣接駅ペア (line_cd, station_cd1, station_cd2)

結合と抽出の要点:

- 有効データは e_status = 0 のみ (廃駅は close_ymd / e_status で判別可能)
- 路線の駅リスト: station.line_cd = X を e_sort 昇順で
- 路線名・事業者名: line_cd → line_name、company_cd → company_name
- 座標列は lon, lat の順で格納されている点に注意 (経度が先)
- 新幹線・地下鉄・ケーブル等の除外は line_name の文字列で判定 (プロトタイプ実績)

# 線形取得パイプライン (実証済み)

OpenStreetMapのOverpass APIから路線の実形状を取得する。line_cd単位で一度取得してDB/ファイルにキャッシュすれば全ユーザー・全便で共有できる (610路線のみ)。

手順:

1. Overpassへクエリ。2パターンを試す:
   - route relation: `relation["route"="train"]["name"~"<路線名>"];out geom;` — メンバーが順序付きなので結合が容易。環状線・複線に強い (山手線で実証)
   - way検索: `way["railway"="rail"]["name"~"<路線名>"];out geom;` — relationがない路線用 (名松線で実証)。OSM上の名称は「南海電気鉄道加太線」のように事業者名付きの場合があるため部分一致 (~) を使う
2. 単純な路線は端点一致でway連結 (キーは lat/lon を7桁でtoFixedした文字列)
3. 他路線と線路を共有する区間がある場合 (例: 加太線の和歌山市〜紀ノ川は南海本線を走行)、bbox指定で関係する全路線のwayを取得し、全頂点をノードとするグラフを構築して起点駅〜終点駅間をダイクストラ法で探索する (加太線で実証)。注意: bboxで途切れた孤立断片が生じるため、最大連結成分内でグラフの起点・終点を選ぶこと
4. Douglas-Peuckerで間引き (許容誤差 0.00003度 ≈ 3m)。環状線は始点=終点で退化するため、2分割してから各半分に適用する
5. 検証: (a) 各駅を線形上に射影 (線分への垂線射影で弧長位置を計算) し、駅順で弧長が単調増加すること (b) 駅と線形の距離が数十m以内であること (c) 総延長が実路線長とほぼ一致すること (山手線34.5km、名松線43.5km、加太線12.0kmで確認済み)
6. 線形が駅の手前で終わる場合 (OSM上の路線起点が分岐点のため)、駅座標まで線を延長する (名松線の松阪側で実施)

運用上の注意:

- overpass-api.de はレート制限がある (連続アクセスで429、XMLエラーページが返る)。リトライ間隔を空ける、ミラー (overpass.kumi.systems等) を用意する、キャッシュ前提の設計にする
- Overpassが返すのはJSONだがエラー時はXML。Content判定を必ず行う

# 日記生成 (LLM)

夜間バッチで翌朝の便を生成する。リアルタイム性が不要なのでBatch API (半額) が使える。品質が必要なのは日記文のみなので、モデルは中〜軽量級から検証する。

プロンプトに含めるもの:

- ペルソナ定義: 名前、種族/属性、性格、興味の対象、文体 (例: 無口な猫→短文・体言止め / のんびり屋→饒舌で脱線する)
- 路線コンテキスト: 路線名、事業者、駅リスト (順序付き)、路線の歴史・特徴 (Wikipedia路線記事から取得)
- 駅周辺の実在スポット: Wikipedia駅記事・OSMのPOIから取得して渡す。日記の質はここで決まる。定型文っぽくなると一気に冷めるため、実在の固有名詞 (名物・史跡・河川・橋梁・廃線跡など) を必ず織り込む

生成ルール (プロトタイプで確立):

- stop (途中下車) は3〜6文でエピソード性を持たせる。pass (車窓から) は1〜2文で軽く
- ローカル線ではダイヤの疎らさを反映する (途中下車したら次の列車まで数時間、という時間経過を日記に入れるとリアリティが出る)
- 固有名詞リンク: Wikipediaに確実に記事がある語は `https://ja.wikipedia.org/wiki/<記事名>`、不確実な語は `https://www.google.com/search?q=<検索語>`。日記textにaタグとして埋め込む (target=_blank, rel=noopener)
- 駅名リンクはビューア側で自動生成: `https://ja.wikipedia.org/wiki/Special:Search?search=<駅名>駅` (同名駅の曖昧さ回避のためSpecial:Search経由)
- 事実の捏造をしない。自信がないスポットは書かないか、検索リンクに逃がす

# ビューア (参照実装あり)

reference/ の3本のHTMLがUI/UXの正解例。単一HTML・依存はLeaflet (CDN) のみ。

- 左: Leaflet地図。実線形ポリライン + 駅circleMarker + 分身マーカー (絵文字divIcon)
- 右: 日記フィード。カードは stop / pass で見た目を変える
- 「旅を再生」で分身が線形上をイージング移動しながらカードが時系列で出現、カードクリックで該当地点へ地図ジャンプ、「全部読む」で一括表示
- 分身の移動は弧長ベース: 累積距離配列 cum を持ち、弧長→座標の逆引きは二分探索 + 線形補間 (latLngAt関数)。駅の弧長位置は垂線射影 (project関数)
- タイル: OpenStreetMap標準 + 地理院タイル航空写真 (切替、キー不要)
- クレジット表記: 「駅位置: 駅データ.jp / 線形: (c) OpenStreetMap contributors (ODbL)」

# 技術スタック提案

- フロント: 静的サイト (参照実装のvanilla JSをそのまま発展させるか、軽量フレームワーク)。tripのJSONを読むだけ
- バックエンド: 夜間cron 1本 (路線選定 → 線形キャッシュ確認/取得 → スポット情報収集 → LLM生成 → trip JSON保存)。v1ではDBすら不要でJSONファイル + オブジェクトストレージでも成立
- LLM: サーバー側でAPIキー管理。1便 = 1〜2コール。ユーザー別生成 (v2) でも1ユーザー1日1〜2コールでコストは軽微
- 通知: v1はなし。次段でWebプッシュ (iOS PWA対応済み)

# 法務 (確認済み 2026-07-12)

- 駅データ.jp (https://ekidata.jp/agreement.php): 商用利用OK・連絡不要・出典明記不要 (歓迎)。加工利用OK。唯一の制約は「非加工データそのもの」の有償提供の禁止 — 非加工データを有償システムに組み込むのは明示的にOKなので、本サービスは規約上問題なし。データは予告なく更新・提供終了があり得るため、CSVスナップショットを同梱・保持する
- OpenStreetMap: ODbL。「(c) OpenStreetMap contributors」の表記を維持すれば商用OK
- Wikipedia・Google検索へのリンクは制約なし
- 地理院タイル: 出典表記 (地理院タイル) を維持

# プロトタイプからの落とし穴メモ

- Overpassのroute relationは存在しない路線がある (名松線にはなかった)。way検索へのフォールバック必須
- OSMの路線名は表記ゆれあり (「JR山手線」「南海電気鉄道加太線」)。正規表現部分一致で拾い、line CSVの路線名と突き合わせる
- 環状線のDouglas-Peuckerは2分割してから (始点=終点の退化対策)
- 山手線の方向ラベル: 駅データのe_sort順 (大崎→五反田→渋谷→新宿→池袋) は内回り
- 共用区間 (直通運転区間) はどの路線でも起こる。グラフ+ダイクストラ方式を標準パイプラインにするのが安全
- 駅CSVに路線名はない (line_cdのみ)。line CSVとの結合を忘れない

# 参照ファイル

- reference/bunshin_travel_diary.html: 第1便 山手線一周 (環状線、手書き日記の品質基準)
- reference/bunshin_diary_02_meisho.html: 第2便 JR名松線 (ローカル線、固有名詞リンク実装)
- reference/bunshin_diary_03_kada.html: 第3便 南海加太線 (ペルソナ切替、共用区間のダイクストラ線形、駅名リンク実装)
- data/: 駅データ.jp CSV 4本 (スナップショット)
